spring:
  application:
    name: inventory
  datasource:
    url: jdbc:postgresql://localhost:15432/inventory
    username: inventory
    password: inventory
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
